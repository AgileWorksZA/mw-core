#!/usr/bin/env bun run ${PWD}/.claude/scripts/cloudios/register.ts

# Register with CLOUDIOS

Registers this Claude agent with the CLOUDIOS task tracking system.

Usage: `/cloudios:register`

To use a different server:
`export CLOUDIOS_SERVER=http://your-server:4000`