{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "User2",
  "description": "User2 table schema (file_num: 30) - Extended user data",
  "type": "object",
  "properties": {
    "SequenceNumber": {
      "type": "integer",
      "description": "Unsigned long sequence number"
    },
    "LastModifiedTime": {
      "type": "string",
      "format": "date-time",
      "description": "Last modified timestamp"
    },
    "DevKey": {
      "type": "integer",
      "description": "Developer key (indexed, mutable: freely, script-only)"
    },
    "Key": {
      "type": "string",
      "maxLength": 28,
      "description": "Key (indexed, mutable: freely, script-only)"
    },
    "Int1": {
      "type": "integer",
      "description": "Integer field 1 (mutable: freely, script-only)"
    },
    "Int2": {
      "type": "integer",
      "description": "Integer field 2 (mutable: freely, script-only)"
    },
    "Float1": {
      "type": "number",
      "description": "Float field 1 (mutable: freely, script-only)"
    },
    "Float2": {
      "type": "number",
      "description": "Float field 2 (mutable: freely, script-only)"
    },
    "Date1": {
      "type": "string",
      "format": "date",
      "description": "Date field 1 (mutable: freely, script-only)"
    },
    "Date2": {
      "type": "string",
      "format": "date",
      "description": "Date field 2 (mutable: freely, script-only)"
    },
    "Text1": {
      "type": "string",
      "maxLength": 256,
      "description": "Text field 1 (mutable: freely, script-only)"
    },
    "Text2": {
      "type": "string",
      "maxLength": 256,
      "description": "Text field 2 (mutable: freely, script-only)"
    },
    "Text": {
      "type": "string",
      "maxLength": 1024,
      "description": "Long text field (mutable: freely, script-only)"
    },
    "Int3": {
      "type": "integer",
      "description": "Integer field 3 (mutable: freely, script-only)"
    },
    "Int4": {
      "type": "integer",
      "description": "Integer field 4 (mutable: freely, script-only)"
    },
    "Float3": {
      "type": "number",
      "description": "Float field 3 (mutable: freely, script-only)"
    },
    "Float4": {
      "type": "number",
      "description": "Float field 4 (mutable: freely, script-only)"
    },
    "Date3": {
      "type": "string",
      "format": "date",
      "description": "Date field 3 (mutable: freely, script-only)"
    },
    "Date4": {
      "type": "string",
      "format": "date",
      "description": "Date field 4 (mutable: freely, script-only)"
    },
    "Text3": {
      "type": "string",
      "maxLength": 256,
      "description": "Text field 3 (mutable: freely, script-only)"
    },
    "Text4": {
      "type": "string",
      "maxLength": 256,
      "description": "Text field 4 (mutable: freely, script-only)"
    },
    "TaggedText": {
      "type": "string",
      "maxLength": 256,
      "description": "Tagged text (mutable: freely, script-only)"
    },
    "Colour": {
      "type": "integer",
      "description": "Display color (mutable: freely, script-only)"
    }
  },
  "required": [
    "SequenceNumber",
    "LastModifiedTime",
    "DevKey",
    "Key"
  ]
}
