# MoneyWorks Core API Types

Find the types for the MoneyWorks Tables in types/table-names.ts

There are JSON schemas for each table in json-schema/table-name-schema.json

To load memory-optimized schemas for raw data types, use optimized/table-name-schema.ts
