{
  "name": "@moneyworks/monorepo",
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "bun-types": "latest",
    "xml2js": "^0.6.2"
  },
  "private": true,
  "scripts": {
    "build:cli": "bun --cwd packages/cli build",
    "clean": "rm -rf node_modules packages/*/node_modules",
    "test": "bun test",
    "cli": "bun --cwd packages/api cli",
    "mw": "bun --cwd packages/cli dev"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ]
}